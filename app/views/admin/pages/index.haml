#main
  %ul
    - @pages.each do |page|
      %li
        = link_to page.title, edit_admin_page_path(page)
        %br
        = link_to '[edit]', edit_admin_page_path(page)
        = link_to '[delete]', admin_page_path(page), remote: true, method: :delete
